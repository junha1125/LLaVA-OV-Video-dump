# LLaVA-OV-ICLR26 with NAVER

## install 

```
# docker image: nvcr.io/nvidia/pytorch:23.12-py3
pip install --upgrade pip  # Enable PEP 660 support.
pip install -e ".[train]" --default-timeout=9000
```# LLaVA-OV-Video-dump
